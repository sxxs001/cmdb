"""All ``entities`` will be registered here. If you have request some `schema`
and `entity` from the client, the class definition of these `entities` can be
get from this module.

Example::

    from cmdblib import entities

    client.get_entity('test', 'my_entity_key')

    MyTestEntityClassBasedOnTestSchema = getattr(entities, 'test')
    my_new_entity = MyTestEntityClassBasedOnTestSchema(
        {
            "id": "asd",
            "long": "12345",
            "long1": 12345,
            "multi": ["a", "b"]
        }
    )

"""

__author__ = "yuchen.huang"

entities = {}
